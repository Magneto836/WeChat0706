<view class = "page-container">
<image class="circleImage" src="{{userInfo.avatarUrl}}"></image>
<view class="userNickname">{{userInfo.nickName}}</view>

<block wx:if="{{userInfo}}">
  <view style="text-align: center;" >
<text class = "RegistHelpText" >\n请输入昵称（即账号）和密码进行注册\n注：昵称长度不能超过10\n密码长度不能小于3，也不能大于15\n昵称中只能包含中英文和汉字，\n不能包含特殊字符与表情\n密码只能由英文和数字组成</text>
</view>
<view class="label-list">
    <text class="label" space="emsp">昵称  </text>
    <view class="input-box">
        <input type="text" placeholder="请输入昵称" bindinput="getUserAccountId"/>
    </view>
</view>

<view class="label-list">
    <text class="label" space="emsp">密码  </text>
    <view class="input-box">
        <input type="{{passwordFieldType1}}" placeholder="请输入密码" bindinput="getUserPassword"/>
        <button type="default" bindtap="ShowPassword1" class="ShowBtn">显示密码</button>
    </view>
</view>

<view class="label-list">
    <view class="label">确认密码</view>
    <view class="input-box">
        <input type="{{passwordFieldType2}}" placeholder="请确认密码" bindinput="ConfirmUserPassword"/>
        <button type="default" bindtap="ShowPassword2" class="ShowBtn">显示密码</button>
    </view>
</view>
</block>

<button wx:if="{{!userInfo}}" type="default" bindtap="getUserProfile" class="buttonType">微信授权</button>
<button wx:if="{{userInfo}}" type="default" bindtap="register" class="buttonType">注册</button>

</view>